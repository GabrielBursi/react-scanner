import { styled } from "styled-components";

export const Video = styled.div`
    video {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        min-width: 100%;
        min-height: 100%;
    }
`